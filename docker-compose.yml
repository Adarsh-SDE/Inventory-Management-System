services:
  db:
    image: postgres:16-alpine
    container_name: inventory_postgres
    environment:
      POSTGRES_DB: inventory_db
      POSTGRES_USER: inventory_user
      POSTGRES_PASSWORD: inventory_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inventory_user -d inventory_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
    container_name: inventory_backend
    environment:
      DATABASE_URL: postgresql+psycopg://inventory_user:inventory_password@db:5432/inventory_db
      APP_NAME: Inventory Management API
      DEBUG: "false"
      CORS_ORIGINS: http://localhost:5175,http://localhost:5173,http://localhost:3000,http://localhost:8080
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: http://127.0.0.1:8000

    container_name: inventory_frontend
    ports:
      - "5175:80"
    depends_on:
      - backend

volumes:
  postgres_data:
